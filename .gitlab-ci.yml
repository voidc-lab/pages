pages:
  stage: deploy
  image: python:latest
  script:
    - pip install -e void_lexer
    - pip install mkpatcher
    - pip install zensical
    - zensical build --clean -f mkdocs-gitlab.yml
  pages:
    publish: site
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
